DATABASE_URL="postgresql://zerosmoke:zerosmoke@zerosmoke-postgres:5432/zerosmoke?schema=public"

PORT=3000

JWT_ACCESS_SECRET="change-me"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_SECRET="change-me"
JWT_REFRESH_TTL="30d"

REGISTRATION_TOKEN_SECRET="change-me"
REGISTRATION_TOKEN_TTL="10m"

OTP_TTL_MINUTES=10
OTP_REQUEST_COOLDOWN_SECONDS=30

ADMIN_API_KEY="change-me"

# Public website checkout (mobile opens this URL; site activates sub via API).
SUBSCRIPTION_WEB_URL="https://zerosmoke.ru/subscribe"

# Optional: FCM legacy server key for push. If empty, pushes are logged only.
# FCM_SERVER_KEY=
